#[cfg(test)]
mod tests {
    // use crate::tree::*;

    #[test]
    fn temporary() {
        // let tree: Tree = pow('x'.into(), 2.0.into()) + pow('y'.into(), 2.0.into());
        // let mut storage = DepthIterStorage::from(&tree);
        // let mut iter = DepthIterator::from(true, &storage);
        // println!("============================");
        // for node in iter {
        //     println!("{:?}", node);
        // }
        // println!("============================");
        // let tree: Tree = sqrt('x'.into()) + sqrt('y'.into());
        // for node in iter {
        //     println!("{:?}", node);
        // }
        // println!("============================");
        // assert!(false);
    }
}
